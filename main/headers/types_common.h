
#pragma once

// Application screens
typedef enum {
    SCREEN_HOME,
    SCREEN_CHAT,
    SCREEN_ABOUT
} screen_t;

// Generic status
typedef enum {
    STATUS_OK,
    STATUS_ERROR,
    STATUS_BUSY
} status_t;
