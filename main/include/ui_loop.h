#pragma once

/**
 * @brief Main UI loop - call this in your main application loop
 * This function handles continuous UI updates, input processing,
 * and screen state management.
 */
void ui_main_loop(void);
